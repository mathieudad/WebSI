const React = require('react');
const InputMessage = require('InputMessage').InputMessage;

function Frame () {
    return(
        <div>

        </div>
    )
}

module.exports = {
    Frame : Frame
}